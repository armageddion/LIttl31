// Click/tap pin for the pricing-tier info popover — hover/focus-within
// handles pointer devices via Tailwind's group-hover, this covers touch,
// where there's no hover state to rely on.
.pricing-info-popover.is-pinned
	opacity 1
	pointer-events auto
